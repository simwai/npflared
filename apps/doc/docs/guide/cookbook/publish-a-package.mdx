import { PackageManagerTabs } from '@theme';

# Manifesting a Library (Publishing)

In this cognitive walkthrough, we will observe how to manifest a shared library into your Babadeluxe-registry node. This process allows for the seamless sharing of private artifacts within **BabaDeluxe's** collective intelligence 🤝.

## 1. Initializing the Artifact
Begin by creating the substrate for your shared library.

<PackageManagerTabs command="init" />

Add your logic to the `index.js` file:

```js title="index.js"
export const add = (a, b) => a + b;
export const sub = (a, b) => a - b;
export const mul = (a, b) => a * b;
export const div = (a, b) => a / b;
```

## 2. Configuring the Connection
Update your `package.json` to signal where the library should be manifest.

```json {6-9} title="package.json"
{
  "name": "@acme/math",
  "version": "1.0.0",
  "description": "A mathematical utility for our mesh",
  "main": "index.js",
  "publishConfig": {
    "access": "public",
    "registry": "https://your-babadeluxe-registry.dev"
  },
  "exports": {
    ".": "./index.js"
  }
}
```

Replace `https://your-babadeluxe-registry.dev` with the actual URL of your registry node.

## 3. Establishing the Trust Signal (Token)
If you haven't yet manifested an authentication token, you can do so via the [CLI](/guide/install/cli) or the [Token API](/guide/api/tokens).

## 4. Authenticating the Node
To establish a persistent connection with your registry, add your token to a `.npmrc` file:

```txt title=".npmrc"
//your-babadeluxe-registry.dev/:_authToken=YOUR_SYNERGETIC_TOKEN
```

## 5. Initiating the Publication 🚀
Execute the publication command to manifest your library in the registry.

<PackageManagerTabs command="publish" />

Your artifact is now part of the registry's collective memory, ready to be ingested, upgraded, and consumed by other nodes 🔥.

## 6. Assimilating the Library
To ingest the library into another project, ensure your `.npmrc` is configured correctly, then run:

<PackageManagerTabs command="install --registry https://your-babadeluxe-registry.dev" />

### Scoped Resolution
For scoped packages (e.g., `@acme/math`), you can configure your `.npmrc` to automatically resolve the scope through your private node:

```txt title=".npmrc"
@acme:registry=https://your-babadeluxe-registry.dev
//your-babadeluxe-registry.dev/:_authToken=${BABA_REGISTRY_TOKEN}
```
