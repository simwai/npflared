export type D1Database = {
  id: string;
  name: string;
  createdAt: string;
  version: string;
  numberOfTables: number;
  size: number;
};

export type R2Bucket = {
  name: string;
  createdAt: string;
};
